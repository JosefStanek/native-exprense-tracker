import express from "express";
const router = express.Router();

// register user
router.post("api/register", async () => {});
// login user
router.post("api/login", async () => {});

export default router;
